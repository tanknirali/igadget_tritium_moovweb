# match($path) {
#   # Match the Ajax path
#   with(/foo/) {
#     html_fragment() {
#       @import "ajax/foo.ts"
#     }
#   }
# }

# needed for product images
# replace("%24", "$")
# replace("&amp;", "&")